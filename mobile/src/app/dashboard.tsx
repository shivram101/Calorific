// src/app/dashboard.tsx
// The dashboard IS the diary — one homepage with numbers, goals, log, and
// water together (matches the web flow). This alias keeps /dashboard as a
// valid route.
export { default } from "./diary";
